# 林业模拟器 P0：开始界面与野外调查重做

版本：v0.1 玩法与 UI 提案

状态：仅整理界面、存档、探索玩法和数值，尚未修改游戏代码

关联文档：[蜂种、木材、果树与扩展玩法整理](forestry-new-gameplay-expansion.md)

## 1. P0 目标

本轮 P0 解决两个基础问题：

1. 游戏启动后直接进入复杂工坊，没有正式的开始、读档和关于入口；
2. 当前探索只需点击一次按钮，消耗 4～5 能源后立即随机结算，区域之间缺少难度、路线和玩法差异。

新的进入与探索流程：

```text
启动游戏
  → 开始界面
  → 新游戏 / 继续游戏 / 加载存档
  → 进入生态工坊
  → 选择探索区域
  → 二级调查确认窗口
  → 手动调查小游戏 / 自动探索队列
  → 调查结算与区域进度
```

所谓“小游戏窗口”统一实现为当前页面中的全屏对话层，不使用 `window.open()`。这样不会被浏览器拦截，也可以在 Android WebView 和桌面浏览器中复用同一套 HTML、CSS 与 JavaScript。

## 2. 多端通用开始界面

### 2.1 开始界面按钮

有存档时的按钮顺序：

1. 继续游戏：读取最近使用的有效存档；
2. 新游戏：选择存档槽并开始新的林业工坊；
3. 加载存档：打开三个存档槽、导入和导出功能；
4. 关于：查看版本、玩法说明、版权声明和外部资料入口。

没有存档时隐藏“继续游戏”，把“新游戏”设为默认焦点；“加载存档”仍可用于导入其他设备的存档。

### 2.2 桌面端布局

```text
┌──────────────────────────────────────────────────────────┐
│  FORESTRY LAB                              VERSION 0.x    │
│                                                          │
│       像素森林、蜂巢和树冠组成的静态 CSS 背景            │
│                                                          │
│                 ┌────────────────────┐                   │
│                 │   林业生态实验室   │                   │
│                 │  FORESTRY LAB      │                   │
│                 │                    │                   │
│                 │  [ 继续游戏 ]      │                   │
│                 │  [ 新游戏   ]      │                   │
│                 │  [ 加载存档 ]      │                   │
│                 │  [ 关于     ]      │                   │
│                 └────────────────────┘                   │
│  本地存档 · 自动保存    Minecraft · Forestry · Wiki · Git│
└──────────────────────────────────────────────────────────┘
```

- 主面板宽度建议为 360～420px；
- 四个按钮保持相同宽度和 48px 以上高度；
- 背景只使用低对比度 CSS 方块、树冠和蜂巢轮廓，不与按钮争夺焦点；
- 版本号与存档状态在左下角，外部资料链接在右下角。

### 2.3 手机竖屏布局

```text
┌──────────────────────┐
│ FORESTRY LAB   v0.x  │
│                      │
│   像素树冠 / 蜂巢    │
│                      │
│  林业生态实验室      │
│  [ 继续游戏       ]  │
│  [ 新游戏         ]  │
│  [ 加载存档       ]  │
│  [ 关于           ]  │
│                      │
│ Minecraft · Mod Wiki │
│ Forestry · Source    │
└──────────────────────┘
```

- 标题和装饰图占首屏高度不超过 34%；
- 按钮单列排列，左右边距至少 20px；
- 外部链接从“屏幕角落”改为底部两行，避免小屏横向越界；
- 横屏和高度不足时允许整体垂直滚动，但主按钮不能被固定页脚遮挡；
- Android 返回键在开始界面触发退出确认，在子弹窗中只关闭当前弹窗。

### 2.4 视觉风格

- 延续现有深绿色、灰白面板和琥珀色强调色；
- 标题使用当前 Minecraft 风格像素字体；
- 所有边框使用直角和 2～4px 阶梯阴影；
- 不直接复制 Minecraft 标题 Logo；
- 蜂、树、蜂巢和方块背景使用项目自己的 CSS 像素图形；
- 禁止持续背景动画，移动端不增加无意义耗电。

## 3. 存档入口与安全规则

### 3.1 三个本地存档槽

每个存档槽显示：

- 存档名称；
- 当前章节和工坊等级；
- 蜂种、树种与蝶种发现数量；
- 游戏时长；
- 最后保存时间；
- 当前区域和一张 CSS 像素缩略图；
- 存档版本和是否需要迁移。

空槽显示“建立新工坊”。损坏的槽位不直接删除，显示“无法读取”，并保留导出原始数据的入口。

### 3.2 新游戏

点击“新游戏”后：

1. 选择存档槽；
2. 输入工坊名称，默认“森林工坊 01”；
3. 查看初始资源和第一章目标；
4. 确认创建。

若目标槽已有存档，必须出现第二次覆盖确认，并提供“先导出备份”。确认文字要写明存档名称，不能只显示“是否确定”。

### 3.3 加载、导入与导出

- 点击存档槽主体加载；
- 每个槽提供导出 JSON、重命名和删除按钮；
- 删除存档需要输入或点击第二次明确确认；
- 导入时先验证版本、字段类型和文件大小，再选择目标槽；
- 导入不得执行文件中的 HTML、脚本或外部链接；
- Web 端使用下载与文件选择器；
- Android 端使用系统文件选择器或分享面板；
- 导出文件名建议为 `forestry-lab-存档名-日期.json`。

### 3.4 自动保存

以下情况立即保存：

- 完成探索结算；
- 启动或收取生产；
- 完成杂交、升级、交易或委托；
- 切换回开始界面；
- 页面进入后台。

另设 15 秒防抖自动保存。界面右上角短暂显示“已保存”，不使用持续闪烁图标。

### 3.5 旧存档迁移

当前单存档键 `forestry-lab-prototype-v1` 在第一次加载新版本时迁移到存档槽 1，并命名为“旧版工坊”。迁移步骤：

1. 读取并验证旧数据；
2. 写入新存档槽；
3. 再次读取并校验新槽；
4. 标记迁移完成；
5. 暂时保留旧键，不立即删除。

只有玩家成功进入并完成一次新格式保存后，才允许清理旧键，避免迁移失败导致存档丢失。

## 4. 关于页面与外部入口

### 4.1 关于页面内容

- 游戏名称和版本；
- “受 Minecraft Forestry 模组启发的非官方模拟小游戏”；
- 核心玩法简介；
- 本地存档与隐私说明；
- 使用的字体、图标和资源许可说明；
- 制作人员和开源组件；
- 外部资料链接。

必须明确说明本项目不是 Mojang、Microsoft 或 Forestry 团队的官方产品。

### 4.2 开始界面角落链接

| 显示名称 | 地址 | 定位 |
| --- | --- | --- |
| Minecraft 官网 | <https://www.minecraft.net/> | Minecraft 官方入口 |
| Forestry 模组页 | <https://www.curseforge.com/minecraft/mc-mods/forestry> | 模组介绍、版本与下载信息 |
| Forestry 社区 Wiki | <https://ftbwiki.org/Forestry> | 社区整理的玩法资料 |
| Forestry 1.12 源码 | <https://github.com/ForestryMC/ForestryMC/tree/mc-1.12> | 当前小游戏主要参考版本 |

Forestry 仓库中列出的旧 Wiki 域名可用性不稳定，因此开始界面同时保留 CurseForge 和 1.12 源码入口。外部地址统一存放在配置表中，不把 URL 散落写入多个界面。

Web 端使用新标签页并添加 `rel="noopener noreferrer"`；Android WebView 遇到允许列表中的外部域名时交给系统浏览器打开。离线时保留链接文字，并提示“当前无法打开外部页面”。

## 5. 新的区域调查流程

### 5.1 从区域卡到结算

```text
点击区域卡的“探索”
  → 打开二级调查确认窗口
  → 检查能源、仓库、难度和奖励池
  → 选择手动调查或自动探索
  → 在确认按钮按下时一次性扣除能源
  → 生成并保存本次调查种子
  → 完成小游戏或等待自动探索
  → 进入统一结算窗口
```

区域卡本身不再直接扣能源或随机发放物品。这样可以避免误触，也能让玩家在手机上看清本次成本。

### 5.2 二级调查确认窗口

确认窗口固定显示：

- 区域名称、难度等级和环境标签；
- 当前区域调查次数、熟练度和稀有发现进度；
- 主要资源、可能物种和花源；
- 当前仓库剩余空间；
- 手动与自动两种模式的能源消耗；
- 收获倍率、稀有发现倍率和预计耗时；
- 自动探索次数：1、3 或 5 次；
- 取消、开始手动调查、启动自动探索三个操作。

桌面端使用左右两张模式卡；手机端使用上下两张模式卡。确认按钮必须写出完整成本，例如“消耗 12 能源 · 开始手动调查”，而不是只写“确定”。

### 5.3 能源扣除时机

- 打开确认窗口不扣能源；
- 点击最终开始按钮时扣除；
- 扣除与创建调查记录必须是同一次原子操作；
- 手动调查中途撤离不返还能源，但保留已经装入调查包的物品；
- 页面刷新后恢复同一张调查地图，不允许通过刷新重新随机；
- 自动探索按每次任务开始时逐次扣除，能源不足时停止后续队列。

## 6. 手动调查小游戏

### 6.1 核心形式：样方路线调查

手动调查采用回合制 `5 × 5` 像素样方地图，不做战斗和即时反应操作。

玩家从地图边缘的营地出发，只能选择相邻的上、下、左、右格。每次移动会揭示地块并消耗调查点，资源格会自动装入调查包。玩家需要在有限调查点内决定深入寻找稀有线索，还是提前撤离保存稳定收获。

同一张 `5 × 5` 地图在 Web 和 Android 上保持完全相同的规则：桌面端支持鼠标和方向键，手机端支持点击格子，不使用拖拽和悬停作为必要操作。

### 6.2 地块类型

| 地块 | 调查点消耗 | 效果 |
| --- | ---: | --- |
| 林间路径 | 1 | 普通移动，无额外奖励 |
| 资源点 | 1 | 获得木材、花源、果实或树脂 |
| 生态线索 | 1 | 提高稀有发现进度，并提示附近特殊节点 |
| 茂密灌木 | 2 | 可能藏有树苗、蜂巢或蝶种 |
| 泥地、沙地或积雪 | 2 | 区域环境障碍，通常连接高价值地块 |
| 悬崖、深水或虚空 | 不可进入 | 阻挡路线，但不会消耗调查点 |
| 临时营地 | 0 | 起点和撤离点，可随时结束调查 |

地图必须保证至少存在一条能够到达三个资源点并返回的路线，不能生成无解地图。

### 6.3 调查操作

- 移动：进入相邻地块；
- 观察：消耗 1 调查点，揭示当前位置周围最多 3 个未探索地块；
- 标记：为一个已发现但尚未抵达的特殊地块加标记，不消耗点数；
- 撤离：立即结束并带回调查包中的物品；
- 帮助：显示地块图例和当前目标，不暂停或重置地图。

当调查点归零时自动撤离。最后一个动作不能让玩家因无法返回营地而丢失已收集物品；营地代表出发位置，不要求实际走回。

### 6.4 难度如何影响地图

| 难度 | 初始调查点 | 障碍与高耗地块 | 稀有线索 |
| --- | ---: | --- | --- |
| I | 10 | 很少，路线清晰 | 1 个保底线索 |
| II | 9 | 少量泥地、灌木或沙地 | 1～2 个线索 |
| III | 8 | 路线分叉明显，高耗格增多 | 2 个线索 |
| IV | 7 | 障碍较多，需要观察和取舍 | 2～3 个线索 |
| V | 7 | 可见范围低，稀有节点更深 | 3 个线索，至少 1 条高风险路线 |

难度提高的是路线压力和能源成本，不加入随机即死、永久丢失物品或无法预知的额外能源扣除。

### 6.5 手动调查奖励

- 资源数量以实际抵达的地块为准；
- 至少抵达 3 个资源点时增加 1 份区域基础补给；
- 每收集 1 个生态线索，区域稀有发现进度额外增加 4；
- 手动模式稀有节点权重为自动模式的 1.35 倍；
- 首次手动完成一个区域后，该区域自动探索永久增加 5% 常规收获，最多通过熟练度提高到 90%。

## 7. 自动探索

### 7.1 自动探索定位

自动探索用于玩家不想重复小游戏或正在管理生产线时获取稳定补给。它消耗更多能源、收获较少、发现稀有物种更慢，但可以排队并离线推进。

### 7.2 自动探索规则

- 可选择连续执行 1、3 或 5 次；
- 同一时间只能存在一个区域的自动探索队列；
- 每次任务分别保存区域、奖励池、能源成本和随机种子；
- 常规资源约为手动调查合理路线的 80%；
- 稀有节点权重为手动模式的约 74%；
- 每次增加 8 点稀有发现进度，手动调查基础增加 16 点；
- 自动探索只能从当前已经实现的物品池中生成奖励；
- 自动探索完成后，奖励进入待领取调查箱，玩家确认后再转入仓库；
- 离线最多推进 5 次，超过部分保持排队状态；
- 能源低于玩家设置的探索保留线或仓库无法容纳最低奖励时自动暂停。

### 7.3 两种模式对比

| 项目 | 手动调查 | 自动探索 |
| --- | --- | --- |
| 操作 | 完成 `5 × 5` 路线小游戏 | 设置次数后等待 |
| 能源 | 使用区域基础成本 | 约为手动的 1.3 倍 |
| 常规收获 | 由路线决定，合理路线按 100%～125% | 约 80%，熟练后最高 90% |
| 稀有权重 | ×1.35 | ×1.00 |
| 稀有进度 | 基础 +16，线索额外 +4 | 每次 +8 |
| 目标选择 | 可以追踪木材、花源、蜂巢或样本 | 系统按区域权重分配 |
| 中途停止 | 可撤离并保留已收集物品 | 当前任务完成后停止队列 |
| 离线推进 | 不推进 | 最多完成 5 次 |

## 8. 探索区域、难度与能源

### 8.1 区域总表

手动能源为一次调查的基础成本；自动能源已经取整并直接显示给玩家。

| 区域 | 难度 | 手动能源 | 自动能源 | 自动耗时 | 主要收获 | 关键发现 | 解锁条件 |
| --- | ---: | ---: | ---: | ---: | --- | --- | --- |
| 森林边缘 | I | 6 | 8 | 30 秒 | 橡木、白桦木、野花、蜂蜜脾 | 基础蜂巢、橡树与白桦树苗、春季蝶 | 初始开放 |
| 平原花地 | I | 7 | 9 | 35 秒 | 木材、三叶草、种子、蜂蜜脾 | 草原蜂线索、平原蝶种 | 完成 1 次森林调查 |
| 静谧沼泽 | II | 9 | 12 | 45 秒 | 湿地木材、蘑菇花源、树脂 | 普通蜂、丛林树苗、湿地蝶 | 总调查 3 次并完成 1 次离心 |
| 荒芜沙丘 | II | 10 | 13 | 50 秒 | 仙人掌花源、椰枣、少量木材 | 干旱蜂系、干燥蜂巢 | 获得种子油并完成 1 次树场收获 |
| 热带林冠 | III | 11 | 15 | 55 秒 | 丛林木、热带花、木瓜、树脂 | 热带蜂、丝质蜂巢、柚木树苗 | 手动调查沼泽 1 次并发现丛林树 |
| 寒带针叶林 | III | 12 | 16 | 60 秒 | 落叶松木、松木、耐寒花源、树脂 | 冰冻蜂巢、寒冷蜂系、松树苗 | 发现落叶松并把蜂箱升到 LV.02 |
| 荧光菌洞 | IV | 14 | 18 | 70 秒 | 蘑菇花源、树脂、孢子、覆盖物 | 洞穴蝶种、苔藓或粉状蜂巢 | 观察 3 种蝶并解锁 3 类花源 |
| 末地边境 | V | 18 | 24 | 90 秒 | 末地花源、紫颂果、稀有碎片 | 神秘蜂巢、末地蜂系、稀有花粉 | 蜂箱 LV.03、发现 8 蜂种并完成生物燃料链 |

### 8.2 每次调查的基础资源范围

以下区间表示完成一条合理手动路线时的目标范围，不是打开区域即保证获得全部物品。

| 区域 | 常规资源范围 | 特殊资源范围 |
| --- | --- | --- |
| 森林边缘 | 木材 8～14、野花 3～6 | 蜂蜜脾 0～2、基础树苗 0～2 |
| 平原花地 | 木材 6～10、三叶草 4～8 | 蜂蜜脾 0～2、种子或蝶种线索 0～2 |
| 静谧沼泽 | 木材 5～9、湿地花源 3～6 | 树脂 1～3、丛林树苗或蜂种线索 0～1 |
| 荒芜沙丘 | 木材 2～5、仙人掌花源 4～8 | 椰枣 2～4、干燥蜂巢 0～2 |
| 热带林冠 | 木材 6～12、热带花 3～6 | 木瓜 2～5、丝质蜂巢或热带树苗 0～2 |
| 寒带针叶林 | 木材 8～14、耐寒花源 2～5 | 树脂 1～3、冰冻蜂巢或松树苗 0～2 |
| 荧光菌洞 | 蘑菇花源 5～9、树脂 2～4 | 孢子 1～3、特殊蜂巢或蝶种线索 0～1 |
| 末地边境 | 末地花源 2～4、紫颂果 3～6 | 神秘蜂巢 0～2、稀有碎片或末地蜂线索 0～1 |

如果对应物品、蜂种或树种尚未在游戏数据中实现，该奖励不会生成，也不会以未知 ID 写入存档；系统改从该区域的常规资源池补足价值。

### 8.3 区域进度

每个区域分别记录：

- 手动调查次数；
- 自动探索次数；
- 区域熟练度；
- 稀有发现进度；
- 已收集资源节点；
- 已发现蜂、树与蝶；
- 最佳单次调查收获。

稀有发现进度达到 100 时，下次有效调查必定出现一个尚未发现的区域物种或样本，成功后只重置该区域、该稀有池的保底值。若该区域已经没有未发现内容，进度改为提高轮换商店订单出现该区域物资的概率。

## 9. 调查结算窗口

结算页面按以下顺序显示：

1. 消耗能源和所用模式；
2. 带回的常规资源；
3. 新发现的蜂种、树种、蝶种或花源；
4. 区域熟练度和稀有发现进度变化；
5. 未能进入仓库的物品；
6. “全部入库”“打开仓库”“再次调查”和“返回工坊”。

手动调查使用调查包暂存本局物品，自动探索使用待领取调查箱。仓库空间不足时不能静默丢弃奖励；物品保留在对应暂存区，装满后禁止开始新的同类调查。

首次探索结算还要解释资源去向：蜂巢进入离心机、树苗进入树场、花源维持蜂箱、木材用于建设。之后不再重复展示教学段落。

## 10. 新手引导调整

新游戏的第一章改为：

1. 从开始界面创建工坊；
2. 进入森林边缘调查确认窗口；
3. 默认高亮“手动调查”，但允许选择自动探索；
4. 手动模式只要求走到 3 个资源点，地图不生成高耗地块；
5. 结算时保证获得野花、木材和至少 1 个蜂蜜脾；
6. 返回工坊后继续现有蜂箱、分析和离心引导。

第一轮使用固定教学种子，避免玩家因随机地图没有找到蜂巢而无法启动主循环。第二轮开始使用正常区域随机种子。

## 11. 数据结构建议

### 11.1 存档索引

```json
{
  "version": 2,
  "lastSlot": 1,
  "slots": [
    {
      "id": 1,
      "name": "森林工坊 01",
      "updatedAt": 0,
      "playTime": 0,
      "chapter": 1,
      "valid": true
    }
  ]
}
```

### 11.2 调查状态

```json
{
  "expedition": {
    "id": "survey-0001",
    "zone": "forest",
    "mode": "manual",
    "status": "active",
    "seed": 184271,
    "energyPaid": 6,
    "surveyPoints": 10,
    "position": [2, 4],
    "revealed": [],
    "bag": [],
    "startedAt": 0
  }
}
```

### 11.3 区域记录

```json
{
  "zoneProgress": {
    "forest": {
      "manualRuns": 1,
      "autoRuns": 0,
      "proficiency": 5,
      "rareProgress": 20,
      "bestYield": 18
    }
  }
}
```

地图、奖励和自动探索都必须由保存的 `seed` 生成。读取存档时不能重新生成，以免玩家反复刷新获取更高奖励。

## 12. Web 与 Android 交互要求

### Web

- 开始界面支持键盘 Tab、Enter 和 Escape；
- 调查地图支持方向键和点击；
- 全屏调查层最大宽度 960px，超宽屏不无限放大；
- 外部链接打开新标签；
- 页面刷新后恢复进行中的手动或自动调查。

### Android 竖屏

- 调查格子触控尺寸不低于 48 × 48px；
- `5 × 5` 地图宽度不超过视口，必要时把格子缩到 52px 左右；
- 地图下方固定显示调查点、调查包和撤离按钮；
- 系统返回键先弹出“撤离并保留当前收获 / 继续调查”；
- 外部链接交给系统浏览器，不在 WebView 内覆盖游戏；
- 自动探索通知属于后续增强，P0 只在重新打开游戏时显示完成结果。

### 简化调查选项

关于页面增加“简化调查”开关。开启后把地图路线改为三列资源选择卡，每轮从三张卡中选择一张，共选择 5 次；能源、奖励、稀有进度和手动模式倍率保持不变，供小屏设备或不方便操作格子的玩家使用。

## 13. 防重复结算与异常规则

1. 调查按钮提交后立即禁用，防止双击重复扣能量；
2. 能源扣除、调查记录和随机种子必须同时写入；
3. 结算记录带唯一 ID，同一 ID 只能领取一次；
4. 手动调查刷新页面后恢复地图、位置、点数和调查包；
5. 自动探索关闭页面后按保存时间推进，但最多完成 5 次；
6. 存档时间倒退时不增加额外离线进度；
7. 仓库不足时奖励进入暂存区，不能丢弃或重复生成；
8. 区域锁定条件在调查开始前和结算时均保留快照，途中升级不会替换奖励池；
9. 新版本删除某个物品时，旧调查包中的该物品转成“待迁移物资”，不能让存档加载失败；
10. 从开始界面删除正在调查的存档时，仍需执行存档删除二次确认。

## 14. P0 实施顺序

1. 建立存档索引、三个槽位和旧存档迁移；
2. 实现开始界面、继续、新游戏、加载和关于；
3. 加入外部链接允许列表和多端打开方式；
4. 重构探索为“确认后创建调查”，移除区域卡直接结算；
5. 接入调查状态、随机种子和统一结算；
6. 实现森林教学地图和手动调查小游戏；
7. 实现自动探索、次数队列和离线推进；
8. 扩展八个区域、奖励池、熟练度和稀有保底；
9. 接入调查包、待领取调查箱和仓库检查；
10. 完成 Web、Android 竖屏、键盘和简化调查测试。

## 15. 验收条件

1. 没有存档、有一个存档、三个满存档和损坏存档时，开始界面状态均正确；
2. 新游戏覆盖已有槽位前必须出现明确二次确认；
3. 旧版单存档可以迁移到槽位 1，资源与进度不丢失；
4. 360px 宽手机和 1920px 桌面均无按钮、链接和存档文字越界；
5. 点击区域卡不会立即扣能源，只有最终确认才扣除一次；
6. 同一区域手动和自动的能源、收获与稀有进度符合表格；
7. 手动地图总能到达至少三个资源点，不生成无解局；
8. 调查中刷新页面不能重置地图、返还能量或重复抽奖；
9. 自动探索能源不足、仓库不足或达到保留线时会暂停；
10. 未实现的物品不会作为奖励写入存档；
11. 调查奖励仓库不足时进入暂存区，不丢失也不重复领取；
12. Minecraft、Forestry、Wiki 和源码链接在 Web 与 Android 上均交给正确的外部浏览器；
13. 关于页面包含非官方项目声明和资源许可说明；
14. Android 返回键、浏览器 Escape 和触屏撤离都不会误删存档或重复结算；
15. 第一轮固定教学调查能稳定获得启动蜂箱循环所需的资源。

## 16. 已核对的外部入口

- Minecraft 官方网站：<https://www.minecraft.net/>
- Forestry CurseForge 模组页：<https://www.curseforge.com/minecraft/mc-mods/forestry>
- Forestry 1.12 源码：<https://github.com/ForestryMC/ForestryMC/tree/mc-1.12>
- Forestry 社区 Wiki：<https://ftbwiki.org/Forestry>
