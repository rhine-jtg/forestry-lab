# 林业河谷地图：分层美术与资产制作规范

版本：v0.1  
状态：设计文档，暂不修改游戏代码  
对应方案：[林业河谷总览地图设计](forestry-world-map-design.md)  
对应概念图：[地图预览提示词与结果观察](forestry-world-map-preview-prompt.md)

## 1. 文档目标

本文件把林业河谷地图拆成可独立生成、替换、动画和响应式显示的资产层，解决以下四类需求：

1. 底部静态地图：春、夏、秋、冬四个版本。
2. 不同建筑：未建设、不同等级、四季版本。
3. 散步村民：可循环播放的连续动作图。
4. UI 弹窗：地点、事件、任务、NPC 和建造信息。

最终画面由前端按坐标组合，而不是把所有内容画死在一张大图里。概念预览图只用于确定氛围和空间关系，不能直接裁切为正式素材。

## 2. 统一画布与像素规则

### 2.1 逻辑坐标

- 河谷逻辑画布：`1536 × 864`，比例为 16:9。
- 建议像素源画布：`768 × 432`，运行时使用 2 倍最近邻放大。
- 所有地图、建筑、热点、NPC 路径和事件锚点都使用 `1536 × 864` 逻辑坐标。
- 坐标原点位于左上角，X 向右，Y 向下。
- 地图外圈保留至少 48 逻辑像素安全边距。
- Web 与 Android 共用同一坐标，不为竖屏重新排列建筑。

### 2.2 镜头

- 桌面全谷：显示完整 16:9 河谷。
- 桌面分区：以锚点为中心放大到 1.35～1.6 倍。
- Android 竖屏：显示同一地图的局部裁切，可切换蜂林、林木、生产、商业四个镜头。
- 镜头移动只改变视口，不改变资产位置。
- 地图缩放必须使用整数倍或保持最近邻，避免像素边缘发糊。

### 2.3 透视与光照

- 采用俯视偏斜的三分之四视角。
- 所有建筑正面统一朝向左下或下方，不允许不同素材自行旋转。
- 主光源固定来自左上方。
- 接触阴影统一向右下偏移。
- 四季只改变颜色、植被、积雪和水体，不改变光源方向。

## 3. 图层堆叠顺序

| Z 层 | 图层 | 内容 | 是否交互 |
| ---: | --- | --- | --- |
| 0 | 季节底图 | 地形、道路、河流、山体、空建筑地块 | 否 |
| 10 | 地表装饰 | 落叶、花瓣、雪堆、水洼等可选季节细节 | 否 |
| 20 | 建筑阴影 | 建筑与大型设施的接触阴影 | 否 |
| 30 | 建筑主体 | 工地、LV.1、LV.2、LV.3 建筑 | 是 |
| 40 | 物流物件 | 木箱、蜂巢箱、原木车、果篮、商队 | 否 |
| 50 | NPC 与动物 | 村民、研究员、蜜匠、蜂群、蝴蝶 | 是 |
| 60 | 环境效果 | 烟雾、灯光、花粉、水花、天气 | 否 |
| 70 | 透明热点 | 建筑、出口、NPC 的点击区域 | 是 |
| 80 | 地图标记 | 待收取、运行、阻塞、任务、升级 | 是 |
| 90 | 地点详情 | 建筑详情卡、事件卡、NPC 对话 | 是 |
| 100 | 全局 HUD | 资源栏、筛选、导航、保存状态 | 是 |

实现时每个层级使用独立容器。地点热点不能通过读取图片颜色判断，必须由配置数据定义。

## 4. 第一层：四季静态地图

### 4.1 底图包含

四季底图只包含：

- 草地、裸土、农田和预留建筑地块；
- 固定道路、桥梁、台阶和广场；
- 河流、湿地、池塘和瀑布；
- 山体、洞穴外壁、远方出口地貌；
- 不会随建筑升级变化的围栏、路灯底座和地标石；
- 地形树木与远景森林，但不包含可升级树场或果园主体。

底图不包含：

- 任何可升级建筑；
- NPC、动物、运输物和状态图标；
- 可收取作物、蜂箱和机器；
- UI、文字、地点名称；
- 建筑投下的阴影。

### 4.2 四季共同约束

四张底图必须逐像素对齐：

- 道路中心线、河岸、桥梁、山体和建筑地块完全相同。
- 建筑地块不得因季节增加树木、雪堆或花草遮挡。
- 水域形状不改变；冬季只增加冰面表现。
- 所有出口位置、地图边界和镜头锚点保持一致。
- 四张图使用同样的透明度、画布尺寸和裁切范围。


### 4.3 四季视觉规范

| 季节 | 地表与植被 | 水体与天气 | 重点色 | 避免 |
| --- | --- | --- | --- | --- |
| 春 | 嫩绿草地、少量泥地、樱色与白色花瓣、树木新芽 | 水位稍高、空气清透 | 嫩绿、浅黄、粉白 | 满屏花朵、过度粉色 |
| 夏 | 深绿树冠、茂盛花圃、农田成熟、道路干燥 | 蓝绿色水面、强但柔和的日光 | 深绿、蜂蜜金、青蓝 | 过暗森林、强烈热浪 |
| 秋 | 黄橙树冠、收获后的田地、道路落叶、果园丰收 | 水位正常、暖色斜光 | 琥珀、橙棕、暗绿 | 全图同一橙色、遮住道路 |
| 冬 | 雪地、常绿针叶、裸枝、道路被清扫出通道 | 河岸结冰、浅水薄冰、冷空气 | 雪白、冷青、深松绿 | 全白无层次、道路消失 |

季节不是现实时间，而是游戏内生产周期。推荐每 8～12 个完整生产周期切换一次，并提前一个周期在地图 HUD 显示季节预告。

### 4.4 底图文件

```text
assets/map/base/
  valley-base-spring.png
  valley-base-summer.png
  valley-base-autumn.png
  valley-base-winter.png
```

建议导出：

- 工作源：768×432 PNG，无损。
- 游戏图：1536×864 PNG 或无损 WebP。
- 预览图：1536×864 JPEG/WebP，仅用于文档。
- 不使用 SVG 自动描摹像素底图。
- 四季文件的地图内容边界与透明/不透明区域必须完全一致。

### 4.5 季节切换

切换顺序：

1. 预加载下一季底图和当前建筑的下一季素材。
2. 暂停 NPC 路径生成，不暂停游戏计时。
3. 底图与建筑在 400～700ms 内同步交叉淡入。
4. 更新季节地表装饰与环境效果。
5. 恢复 NPC，并写入“季节已变化”的地图日志。

低动态模式取消交叉淡入，直接在同一帧替换全部季节层，不能出现底图已入冬而建筑仍是夏季的混合状态。

## 5. 第二层：建筑资产

### 5.1 建筑状态轴

每个建筑至少提供四个建设状态：

| 状态 | 说明 | 可见元素 |
| --- | --- | --- |
| site | 未建设/待解锁 | 空地、木桩、材料箱、条件木牌 |
| lv1 | 基础建筑 | 小型木石建筑、一个功能标志 |
| lv2 | 扩建设施 | 增加侧房、储物、管线或第二层 |
| lv3 | 完整设施 | 明确地标、完整功能区、灯光与旗帜 |

不要为“运行、待收取、阻塞”重新绘制整座建筑。这些状态使用独立覆盖层：

- working：局部灯光、机器烟雾或活动部件；
- ready：金色产物标记；
- blocked：红棕色箱体与停止运输；
- upgrade：金色锤子或脚手架；
- quest：卷轴、NPC 或告示牌。

这样建筑素材数量由“等级 × 四季”决定，不再乘以所有运行状态。

### 5.2 建筑清单

| ID | 建筑 | 规模 | 等级变化重点 |
| --- | --- | --- | --- |
| station | 中央工作站 | 大 | 临时站房 → 两层工作站 → 林业总部 |
| apiary | 古树蜂林 | 中 | 野外蜂箱 → 养蜂棚 → 大型蜂房 |
| arbor | 林木培育场 | 大 | 苗圃 → 自动树场 → 多树种林场 |
| orchard | 果园与花圃 | 大 | 小果园 → 混栽果园 → 生态果园 |
| butterfly | 蝴蝶园 | 中 | 网棚 → 蝶园 → 林冠观察塔 |
| processing | 加工工坊 | 大 | 单机房 → 动力工坊 → 自动加工厂 |
| warehouse | 仓库能源站 | 大 | 木仓 → 分类仓库 → 物流能源中心 |
| market | 村民商店 | 中 | 摊位 → 商店 → 区域贸易所 |
| contracts | 委托驿站 | 小 | 告示牌 → 驿站 → 林业协会 |
| genetics | 遗传研究所 | 中 | 帐篷实验桌 → 实验室 → 研究院 |
| archive | 生态档案馆 | 中 | 记录桌 → 档案馆 → 生态博物馆 |
| greenhouse | 林冠温室 | 大 | 施工地块 → 玻璃温室 → 生态穹顶 |

首版只制作 station、apiary、arbor、processing、warehouse、market、archive 七座建筑。

### 5.3 建筑季节矩阵

每座完整建筑的正式交付矩阵：

```text
site × spring / summer / autumn / winter
lv1  × spring / summer / autumn / winter
lv2  × spring / summer / autumn / winter
lv3  × spring / summer / autumn / winter
```

即每座建筑 16 张透明 PNG。首版七座建筑共 112 张；完整十二座建筑共 192 张。

若工作量过大，可以采用“结构主体 + 季节覆盖”优化，但必须满足：

- 覆盖层不能改变建筑锚点和轮廓尺寸。
- 冬季积雪、春季花藤、秋季落叶和夏季植被独立导出。
- 结构层和季节层之间不能出现透明缝。
- 最终在四季组合测试中达到完整版本相同的观感。

### 5.4 建筑画布与锚点

建筑按规模使用固定透明画布：

| 规模 | 像素源画布 | 逻辑显示画布 | 推荐主体占比 |
| --- | --- | --- | --- |
| 小 | 96×80 | 192×160 | 70%～82% |
| 中 | 128×112 | 256×224 | 72%～84% |
| 大 | 160×136 | 320×272 | 74%～86% |

规则：

- 所有等级和季节使用该建筑同一个画布。
- 锚点固定为画布底边中心，记录为 `anchor: 50% 100%`。
- 主体升级可以向上和两侧扩张，但地面接触点不移动。
- 透明边距不得随季节裁切。
- 接触阴影可以包含在建筑图中，但方向和不透明度必须统一。
- 建筑入口必须朝向相连道路。
- 未建设状态也占用完整画布，保证点击区与未来建筑一致。


### 5.5 建筑季节差异

建筑结构在同等级内保持一致，季节变化只来自环境附着：

- 春：嫩叶、少量花藤、湿润地基、浅色布旗。
- 夏：浓密藤蔓、打开的工作区、深绿植被、明亮遮阳棚。
- 秋：落叶、收获箱、暖色布旗、烟囱和灯光稍明显。
- 冬：屋顶积雪、结霜玻璃、清扫后的入口、室内暖光。

机器、门、招牌和功能图标不能因季节改变位置。冬季积雪不能遮住建筑等级特征或点击入口。

### 5.6 建筑文件命名

```text
assets/map/buildings/<building-id>/
  <building-id>-site-spring.png
  <building-id>-site-summer.png
  <building-id>-site-autumn.png
  <building-id>-site-winter.png
  <building-id>-lv1-spring.png
  ...
  <building-id>-lv3-winter.png
  overlays/
    <building-id>-working.png
    <building-id>-ready.png
    <building-id>-blocked.png
    <building-id>-upgrade.png
```

文件名只使用小写英文、数字和连字符。等级使用 `site`、`lv1`、`lv2`、`lv3`，季节使用 `spring`、`summer`、`autumn`、`winter`。

## 6. 第三层：散步村民连续图

### 6.1 格式选择

正式运行素材使用透明 PNG 精灵表，不使用 GIF 作为主运行格式。

原因：

- 精灵表可以按游戏状态暂停、变速和切换方向。
- PNG 像素边缘更稳定，不受 GIF 调色板限制。
- Web 与 Android 可以用同一张图。
- 低动态模式可以固定显示任意一帧。
- 碰撞、脚点和点击区域更容易保持一致。

可以额外导出 GIF/WebP 作为文档预览，但游戏只读取精灵表。

### 6.2 单角色规格

建议源帧尺寸：`32 × 48`。  
逻辑显示尺寸：`64 × 96`。  
脚点锚点：源坐标 `(16, 44)`，即底部中心上移 4 像素。

步行动画：

- 8 个方向：南、东南、东、东北、北、西北、西、西南。
- 每个方向 6 帧。
- 精灵表排列：6 列 × 8 行。
- 总画布：`192 × 384`。
- 播放速度：7～9 FPS。
- 循环顺序：接触 → 下沉 → 跨步 → 接触 → 下沉 → 跨步。
- 第一帧和最后一帧不能完全重复，避免循环停顿。

待机动画：

- 4 个方向或共用正面方向。
- 每个方向 4 帧。
- 呼吸、眨眼或轻微看向四周，不移动脚点。
- 播放速度：2～4 FPS。
- 每次待机 2～6 秒后再决定是否行走。

### 6.3 角色类型

| ID | 角色 | 配色与道具 | 常驻区域 |
| --- | --- | --- | --- |
| villager | 普通村民 | 棕绿衣服、空手或小篮子 | 市场与中央广场 |
| merchant | 村民商人 | 绿宝石色围裙、账本 | 商店与贸易道路 |
| researcher | 遗传研究员 | 青色外套、样本箱 | 研究所与档案馆 |
| surveyor | 调查员 | 黄褐背包、地图卷轴 | 委托驿站与出口 |
| orchardist | 果园管理员 | 绿色围裙、剪枝工具 | 果园与花圃 |
| hivekeeper | 蜂巢管理员“蜜匠” | 金色蜂巢方块身体、小翅膀 | 蜂房与中央站 |

蜜匠不是普通人形角色，可单独使用 `48 × 48` 帧和 6 帧浮动/扇翅循环。

### 6.4 路径与行为

村民只沿预先定义的道路节点行走，不做自由寻路。

每条路线包含：

```json
{
  "id": "market-to-station",
  "points": [[1180, 700], [1040, 650], [880, 560], [760, 480]],
  "roles": ["villager", "merchant"],
  "seasonal": true,
  "weight": 3
}
```

行为规则：

- 同屏普通村民建议 3～7 名，手机端最多 4 名。
- NPC 到达节点后停留 1～4 秒。
- 两名角色不能在同一条窄路上完全重叠。
- 角色按 Y 坐标动态排序，使走到建筑后方时被正确遮挡。
- 冬季路线避开未清扫支路；暴雨事件可临时关闭湿地路线。
- 进入后台时暂停动画，恢复时不补算每一步，只更新到合理节点。
- 点击角色时停止移动并打开对话，关闭后从当前节点继续。

### 6.5 连续帧验收

- 角色身体高度、头部尺寸和脚点在所有帧保持一致。
- 每帧位移感来自腿、手臂和身体起伏，不允许整个人在画布中抖动。
- 八个方向的服装、道具和颜色保持一致。
- 背面方向不能错误显示正面五官。
- 所有帧使用同一调色板，不出现单帧颜色跳变。
- 最近邻放大 2～4 倍时轮廓仍然清楚。
- 连续播放 30 秒没有明显卡点、闪帧或透明边缘杂色。

### 6.6 精灵表目录

```text
assets/map/characters/
  villager/
    villager-walk.png
    villager-idle.png
  merchant/
    merchant-walk.png
    merchant-idle.png
  researcher/
    researcher-walk.png
    researcher-idle.png
  surveyor/
    surveyor-walk.png
    surveyor-idle.png
  orchardist/
    orchardist-walk.png
    orchardist-idle.png
  hivekeeper/
    hivekeeper-float.png
    hivekeeper-talk.png
```

## 7. 第四层：UI 弹窗

### 7.1 UI 与地图资产分离

UI 弹窗不烘焙进底图或建筑 PNG。弹窗框体可由 CSS 像素边框或九宫格图片组成，文字、数值、按钮和进度条必须由 HTML 渲染。

这样可以：

- 保持中文清晰；
- 动态显示资源和状态；
- 适配不同屏幕宽度；
- 支持键盘、触控和无障碍标签；
- 避免每次文案修改都重新绘图。


### 7.2 弹窗类型

| 类型 | 触发方式 | 桌面建议尺寸 | 手机表现 |
| --- | --- | --- | --- |
| 地图状态气泡 | 建筑异常或完成 | 32～48px 图标 | 同尺寸，扩大透明点击区 |
| 地点详情卡 | 点击建筑 | 320×220～360×300 | 底部抽屉，占宽 100% |
| 建造/升级卡 | 点击工地或升级标记 | 420×300 | 底部确认抽屉 |
| 事件卡 | 点击地图事件 | 480×320 | 接近全屏的底部抽屉 |
| NPC 对话 | 点击 NPC | 620×360～700×440 | 底部对话层，最高 82dvh |
| 世界区域卡 | 点击边缘出口 | 380×300 | 全宽区域卡 |
| 资源流摘要 | 点击运输物 | 300×180 | 小型底部卡 |

### 7.3 地点详情卡结构

```text
┌──────────────────────────────┐
│ 地点图标  古树蜂林      [×]  │
│           养蜂设施 LV.2      │
├──────────────────────────────┤
│ ● 运行中 · 还需 12 秒        │
│ 蜂种：森林蜂                 │
│ 花源：野花 ×4                │
│ 生态适配：82%                │
├──────────────────────────────┤
│ [跟踪目标]      [进入蜂房 →] │
└──────────────────────────────┘
```

规则：

- 标题、状态、原因、主行动四层信息必须齐全。
- 地图弹窗只显示摘要，不复制设施页全部表格。
- 主按钮使用金色，次按钮使用灰绿。
- 锁定地点的主按钮改为“查看解锁条件”。
- 阻塞地点必须提供处理路径，不只显示红色警告。
- 点击地图其他地点时替换当前卡，不叠加多个卡片。

### 7.4 NPC 对话结构

- 左侧/上方：透明背景 NPC 动态图。
- 右侧/下方：NPC 名称、当前一句对话、状态详情。
- 底部：2～4 个话题按钮和一个直接行动按钮。
- 对话图与文字分层，不能把中文画进 NPC 图片。
- 蜜匠使用现有蜂巢管理员形象方向，但后续重绘为统一地图像素比例。
- Android 返回键和 Escape 优先关闭最上层对话。

### 7.5 九宫格 UI 资产

若不用纯 CSS，可提供以下九宫格素材：

```text
assets/map/ui/
  panel-dark-9slice.png
  panel-light-9slice.png
  panel-gold-9slice.png
  button-primary-9slice.png
  button-secondary-9slice.png
  marker-ready.png
  marker-working.png
  marker-blocked.png
  marker-quest.png
  marker-upgrade.png
  marker-locked.png
```

九宫格源图建议为 24×24 或 32×32，边角区域固定为 8 像素。缩放时只拉伸中心和边线，不拉伸角像素。

### 7.6 UI 字体与可读性

- UI 正文、数值和按钮使用项目当前像素字体。
- 地点标题桌面端不小于 16px，手机端不小于 16px。
- 正文与按钮不小于 14px。
- 辅助标签不小于 11px，不能承载唯一解锁条件。
- 普通文字与底板对比度目标至少 4.5:1。
- 弹窗底部不能被 Android 导航和安全区遮挡。
- 状态不能只用颜色区分，必须同时有图标或文字。

## 8. 总资产目录

```text
assets/map/
  base/
    valley-base-spring.png
    valley-base-summer.png
    valley-base-autumn.png
    valley-base-winter.png
  buildings/
    station/
    apiary/
    arbor/
    orchard/
    butterfly/
    processing/
    warehouse/
    market/
    contracts/
    genetics/
    archive/
    greenhouse/
  characters/
    villager/
    merchant/
    researcher/
    surveyor/
    orchardist/
    hivekeeper/
  logistics/
    crate-honeycomb.png
    cart-logs.png
    basket-fruit.png
    cart-trade.png
  effects/
    pollen.png
    smoke.png
    snow.png
    rain.png
    lamp-glow.png
  markers/
    ready.png
    working.png
    blocked.png
    quest.png
    upgrade.png
    locked.png
  ui/
    panel-dark-9slice.png
    panel-light-9slice.png
    panel-gold-9slice.png
    button-primary-9slice.png
    button-secondary-9slice.png
  manifests/
    map-locations.json
    map-paths.json
    map-assets.json
```

概念图继续放在 `assets/concepts/`，不能与正式运行素材混放。

## 9. 地图状态与素材选择

素材选择只读取统一状态：

```text
季节 season
  → 选择四季底图

地点 buildingId + level + season
  → 选择建筑主体

设施状态 working / ready / blocked / upgrade
  → 添加独立覆盖层

NPC role + animation + direction + frame
  → 选择精灵表帧

selectedLocation / activeEvent
  → 显示 HTML/CSS 弹窗
```

状态优先级：

1. 未建设：只显示 site，不显示运行状态。
2. 阻塞：建筑主体 + blocked 标记。
3. 待收取：建筑主体 + ready 标记。
4. 可升级：建筑主体 + upgrade 标记。
5. 运行中：建筑主体 + working 局部效果。
6. 普通：只显示建筑主体。

同一建筑最多同时显示一个主状态标记和一个环境效果。

## 10. 分层生成工作流

### 10.1 第一步：锁定布局

1. 使用现有概念图确定道路、河流、地块和建筑中心。
2. 在 1536×864 逻辑画布上建立地点坐标表。
3. 为七座首版建筑画出统一尺寸的灰盒。
4. 在桌面全谷和 Android 四镜头中检查遮挡。
5. 布局确认后冻结道路、河岸、地块和锚点。

未冻结布局前不批量生成四季与建筑等级素材。

### 10.2 第二步：生成四季底图

生成顺序：

1. 夏季：作为颜色和细节基准。
2. 春季：在夏季固定布局上调整新芽、花期和湿度。
3. 秋季：调整叶色、收获和落叶。
4. 冬季：增加积雪、薄冰和裸枝。

每生成一季，都使用像素差分或半透明叠加检查道路、地块与河岸是否漂移。

### 10.3 第三步：建筑母版

每座建筑先完成夏季 LV.1：

1. 锁定画布、锚点、入口和接触阴影。
2. 由 LV.1 推导 site、LV.2、LV.3。
3. 确认等级变化清楚但仍是同一建筑。
4. 再从四个建设状态派生春、秋、冬。
5. 最后制作 working、ready、blocked、upgrade 覆盖层。

不能每个等级和季节独立生成后直接使用，否则建筑会产生尺寸、透视和入口漂移。生成结果必须经过人工对齐与像素清理。

### 10.4 第四步：角色精灵表

1. 先确定正面角色母版和固定调色板。
2. 绘制南、东、西、北四方向关键姿势。
3. 补齐四个对角方向。
4. 在统一脚点上制作六帧步行循环。
5. 生成精灵表并逐帧检查。
6. 制作待机与对话动作。
7. 在地图路径上以 1×、2×、3×测试清晰度。

AI 图像生成适合做角色方向稿，不适合直接保证 48 帧严格一致。正式精灵表必须经过人工重绘、对齐或逐帧修正。

### 10.5 第五步：UI 弹窗

1. 先用 HTML/CSS 灰盒确定真实文字长度和按钮尺寸。
2. 再制作九宫格边框、状态标记和地点图标。
3. 使用真实中文检查换行和对比度。
4. 在桌面、960px 宽度和 360～400px 竖屏测试。
5. 最后加入 NPC 动图和局部装饰。

## 11. 分层提示词约束

后续生成每类图片时必须包含以下约束。

### 静态底图提示词必须写明

- no buildings；
- no characters；
- no UI；
- no labels or text；
- fixed 16:9 map layout；
- empty reserved construction plots；
- exact same roads, river, cliffs and exits across all seasons。

### 建筑提示词必须写明

- isolated building on genuinely transparent background；
- fixed three-quarter top-down view；
- fixed canvas and bottom-center anchor；
- same building footprint across levels and seasons；
- no ground map, no UI, no text, no character；
- crisp integer pixel art；
- preserve entrance direction and shadow direction。

### 村民连续图提示词必须写明

- sprite sheet, transparent background；
- exact frame grid；
- same character identity, clothes, palette and proportions；
- fixed foot anchor in every frame；
- complete walk cycle；
- no duplicated or missing limbs；
- no frame labels or text。

### UI 弹窗提示词必须写明

- isolated pixel UI frame or component sheet；
- transparent background；
- square corners and stepped shadows；
- no baked-in Chinese text；
- defined nine-slice corners；
- high contrast and Minecraft/Forestry-inspired material language without copied assets。

## 12. 质量验收

### 底图

- 四季画布尺寸、道路、河岸、桥梁、出口和地块完全对齐。
- 建筑预留地没有被季节装饰遮挡。
- 地图在 50% 缩放时仍能区分道路与水体。
- 冬季道路清晰，秋季落叶不遮挡热点。
- 底图不包含任何建筑、NPC、状态或 UI。

### 建筑

- 同一建筑 16 张图使用相同画布和锚点。
- 等级变化一眼可辨，季节变化不改变建筑身份。
- 入口与道路方向一致。
- 未建设状态仍可识别地点用途。
- 透明边缘没有白边、黑边或裁切。
- 状态覆盖层能用于所有等级，不穿过屋顶或地面。

### 村民

- 连续播放没有跳帧和位置抖动。
- 所有方向身份一致。
- 点击区域与脚点跟随角色。
- 角色能被建筑正确前后遮挡。
- 低动态模式固定帧仍然自然。
- 手机端同屏数量受限且不影响触控。

### UI

- 弹窗不遮住选中建筑和关键状态。
- 所有中文由 HTML 渲染且清晰。
- 桌面和手机主按钮位置一致、可触达。
- Escape、Android 返回键和关闭按钮行为一致。
- 九宫格缩放不拉伸角像素。
- 状态图标具备文字替代和可访问名称。

## 13. 首批交付建议

第一批只制作足以验证管线的最小集合：

1. 夏季静态底图 1 张。
2. 中央工作站、蜂房、树场、加工工坊各自的 site、LV.1、LV.2。
3. 每座建筑 ready 与 blocked 覆盖层各 1 张。
4. 普通村民四方向六帧步行精灵表 1 张。
5. 蜜匠浮动循环 1 张。
6. 地点详情卡、NPC 对话框和 6 个状态标记。
7. 桌面全谷与 Android 单分区各一张组合预览。

验证通过后，再扩展四季、LV.3、十二建筑、八方向角色和完整事件效果。

## 14. 关键结论

1. 四季底图只改变视觉，不改变地图几何与坐标。
2. 建筑使用“建设等级 × 季节”，运行状态使用独立覆盖层。
3. 村民正式素材使用透明 PNG 精灵表，不以 GIF 作为运行格式。
4. UI 框体、文字和按钮与地图图片彻底分离。
5. 所有资产共享同一透视、光源、像素倍率和底部中心锚点。
6. 先完成夏季和四座建筑验证分层管线，再批量制作全部资产。

