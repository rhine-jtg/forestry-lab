# 林业模拟器：地图主界面 UI 层级与布局规范

版本：P0 地图主界面提案 v1  
适用平台：Web 桌面端、Web 移动端、Android 竖屏  
文档性质：信息架构与布局规范，不包含代码实现

## 1. 核心结论

玩家启动游戏后直接进入林业谷地地图，不再先停留在独立开始界面。

地图是游戏的常驻根页面，承担以下职责：

1. 展示林业工坊的建设与生态变化。
2. 作为蜂场、树场、加工、商店、档案等玩法的空间入口。
3. 汇总可收取、阻塞、任务、事件和升级状态。
4. 让玩家从地图进入功能，再返回原来的地图位置。

全局游戏 UI 采用上下分布：

- 顶部：身份、关键资源、能源、仓库、时间环境与系统状态。
- 中部：可缩放、可拖动的 2.5D 地图及建筑交互。
- 底部：全局导航、当前目标、常用行动与选中建筑信息。
- 更高层：抽屉、设施工作台、对话、教程和系统弹窗。

本方案替代旧文档中“启动后先显示独立开始界面”和“桌面端常驻左侧页面导航”的默认流程。存档、关于、设置等功能移入地图主界面的系统菜单。

## 2. 启动与进入地图流程

### 2.1 有有效存档

```text
启动游戏
  ↓
读取最近使用的存档
  ↓
恢复地图镜头、选中建筑和未领取状态
  ↓
进入林业谷地地图
```

- 优先加载最近一次有效自动存档。
- 镜头恢复到玩家离开时的位置；如果保存位置无效，则回到中央工作站。
- 恢复后只显示一条简短提示，例如“已加载 3 号工坊”，不弹出阻塞式欢迎窗口。
- 离线生产结算使用地图上方的结算摘要，玩家确认后再逐项领取。

### 2.2 首次进入或没有存档

仍然先显示地图，但使用半透明引导层完成初始化：

```text
地图背景与中央工作站可见
  ↓
建立新工坊引导卡
  ↓
输入工坊名称 / 选择存档槽
  ↓
确认后开启第一段地图教程
```

- 不显示独立标题页。
- 引导卡不能遮挡整个地图，应让玩家先看到未来经营空间。
- 默认值可以直接确认，避免输入成为开始游戏的强制障碍。
- “导入存档”放在引导卡的次级操作中。

### 2.3 存档异常

- 最近存档损坏时进入地图安全模式，地图顶部显示“存档需要处理”。
- 提供“载入其他存档”“尝试修复”“建立新工坊”三个选项。
- 不允许因为单个存档损坏而停留在空白页或无限加载页。

## 3. 总体 UI 层级

```text
GameShell 地图常驻容器
├─ L0  地图背景层
├─ L1  地貌与道路层
├─ L2  建筑、设施与区域入口层
├─ L3  NPC、运输、蜂群与环境表现层
├─ L4  建筑热点、状态角标与选中反馈层
├─ L5  顶部 HUD 与底部主导航层
├─ L6  建筑快捷卡、任务卡与通知层
├─ L7  设施工作台、详情抽屉与 NPC 对话层
├─ L8  确认弹窗、调查小游戏与结算层
├─ L9  新手引导、系统错误与版本迁移层
└─ L10 调试、性能与开发工具层（发行版隐藏）
```

### 3.1 层级职责

| 层级 | 内容 | 是否阻塞地图操作 | 关闭方式 |
| --- | --- | --- | --- |
| L0～L3 | 背景、建筑、NPC、环境动画 | 否 | 不适用 |
| L4 | 点击区、状态角标、选中框 | 否 | 点击空地取消选择 |
| L5 | 顶部资源栏、底部导航 | 否 | 常驻 |
| L6 | 快捷详情、推荐行动、普通通知 | 部分 | 再次点击、滑动或关闭按钮 |
| L7 | 建筑工作台、仓库、商店、对话 | 是 | 返回按钮、Esc、Android 返回键 |
| L8 | 二次确认、调查小游戏、结算 | 是 | 完成流程或明确取消 |
| L9 | 教程强引导、存档错误、迁移确认 | 是 | 完成必要步骤或安全退出 |
| L10 | 锚点、网格、性能数据 | 否 | 开发开关 |

### 3.2 建议层叠顺序

实现时可以使用统一层叠令牌，不在组件内部随意增加极大的 `z-index`：

| 令牌 | 建议值 | 用途 |
| --- | ---: | --- |
| `map-background` | 0 | 地图底图 |
| `map-terrain` | 10 | 水流、道路、季节覆盖 |
| `map-buildings` | 20 | 建筑与设施 |
| `map-actors` | 30 | NPC、蜂群、运输物 |
| `map-hotspots` | 40 | 点击区、状态角标、选中轮廓 |
| `hud` | 100 | 顶部和底部固定 UI |
| `context` | 200 | 建筑快捷卡、提示与通知 |
| `drawer` | 300 | 详情抽屉、设施工作台 |
| `modal` | 400 | 确认、结算、小游戏 |
| `guide` | 500 | 新手教程和强制聚焦 |
| `system` | 600 | 存档错误与系统级提示 |

## 4. 地图主界面的三个固定区域

```text
┌──────────────────────────────────────────────────────────┐
│ 顶部 HUD：身份 / 资源 / 能源 / 仓库 / 环境 / 系统        │
├──────────────────────────────────────────────────────────┤
│                                                          │
│                 2.5D 林业谷地地图                        │
│       建筑 · NPC · 区域出口 · 事件 · 可收取状态           │
│                                                          │
├──────────────────────────────────────────────────────────┤
│ 选中建筑快捷卡 / 当前目标                                │
│ 底部导航：地图 · 调查 · 队列 · 委托 · 仓库 · 更多         │
└──────────────────────────────────────────────────────────┘
```

地图始终是面积最大的区域。顶部和底部 UI 使用实色或高不透明度底板，不能直接把文字压在复杂地图纹理上。

## 5. 顶部 HUD

### 5.1 信息顺序

顶部 HUD 从左到右分为四组：

1. 工坊身份：工坊名称、现场等级、季节或当前章节。
2. 关键资源：绿宝石、能源、仓库占用。
3. 生产摘要：蜂产品、木材、种子油或当前重点资源。
4. 系统状态：天气/环境、自动保存、设置与通知。

### 5.2 默认常显项目

| 项目 | 展示形式 | 点击行为 |
| --- | --- | --- |
| 绿宝石 | 图标 + 当前数量 | 打开商店或货币说明 |
| 能源 | 当前值 / 上限 + 简短进度条 | 打开能源核心 |
| 仓库 | 已用 / 容量 | 打开仓库 |
| 蜂产品 | 蜂蜜或当前主产物数量 | 打开相关库存筛选 |
| 木材 | 当前木材数量 | 打开木材库存筛选 |
| 环境 | 温度、湿度或当前异常图标 | 打开生态摘要 |

顶部不同时常显超过六个资源。其他物品进入仓库详情，资源发生变化时在对应项目旁短暂显示 `+3` 或 `-2`。

### 5.3 状态表达

- 能源不足：青色能源块转为暗红边框，并显示“能源不足”，不能只改变颜色。
- 仓库接近上限：显示“92 / 100 · 接近满载”。
- 自动保存：只短暂显示保存中和保存完成，不持续占据注意力。
- 生态异常：顶部只显示一个最高优先级异常，完整原因进入生态摘要。

## 6. 地图交互层

### 6.1 地图中的对象

| 对象 | 单击/轻触 | 主操作 |
| --- | --- | --- |
| 建筑 | 选中并显示快捷卡 | 进入设施工作台 |
| NPC | 打开对话气泡或对话卡 | 交谈、交付、跳转任务 |
| 区域出口 | 显示区域收益与能源消耗 | 选择手动或自动调查 |
| 可收取物 | 显示数量和来源 | 收取或进入生产队列 |
| 事件标记 | 显示持续时间与影响 | 处理事件或查看详情 |
| 空地 | 取消当前选择 | 继续拖动地图 |

### 6.2 地图操作

- 拖动：平移镜头。
- 滚轮或双指：缩放地图。
- 单击建筑：选中，不立即切走页面。
- 双击或点击“进入设施”：打开设施工作台。
- Esc 或 Android 返回键：先关闭当前浮层，再取消建筑选择，最后才打开退出确认。
- 地图镜头至少提供“全谷、蜂林、林木、生产、商业”五个预设锚点。

移动端不把双击、长按或悬停作为唯一操作方式。

### 6.3 地图状态角标

地图只显示可立即理解的短状态：

- 可收取：产物图标 + 数量。
- 运行中：简短进度环或剩余时间。
- 阻塞：警告图标 + 一个关键词，例如“缺花源”。
- 可升级：向上箭头或锤子图标。
- 委托相关：任务标记。
- 新内容：一次性闪烁后变为静态标记。

完整配方、基因表、价格表和环境公式不能堆在地图上。

## 7. 建筑选择与快捷卡

选择建筑后，在底部导航上方显示快捷卡。

### 7.1 快捷卡内容

```text
┌──────────────────────────────────────┐
│ 古树蜂场 · LV2        运行中 · 68%   │
│ 当前蜂种：草原蜂  花源：野花          │
│ 预计产物：蜂蜜 2 / 蜂蜡 1             │
│ [进入蜂场] [收取] [查看影响]           │
└──────────────────────────────────────┘
```

快捷卡只保留：

1. 建筑名称、等级和当前状态。
2. 一条最重要的生产或阻塞原因。
3. 一个主操作和不超过两个次级操作。

### 7.2 主操作优先级

建筑主按钮按以下顺序决定：

1. 有可收取产物：显示“收取”。
2. 生产被阻塞：显示“修正条件”。
3. 未运行且可启动：显示“开始生产”。
4. 有可用升级：显示“查看升级”。
5. 其他情况：显示“进入设施”。

## 8. 底部 UI

### 8.1 固定主导航

底部默认保留六个全局入口：

| 入口 | 职责 |
| --- | --- |
| 地图 | 返回全谷、取消当前设施页面 |
| 调查 | 打开世界区域图与野外调查 |
| 队列 | 汇总蜂场、树场和加工的运行状态 |
| 委托 | 当前目标、生态委托和成就进度 |
| 仓库 | 库存、容量、分类和出售准备 |
| 更多 | 商店、档案、研究、设置、存档和关于 |

蜂场、树场、加工工坊、商店和档案馆的主要入口仍然是地图建筑。底栏不重复放置所有建筑，避免地图沦为纯装饰。

### 8.2 当前目标条

底栏上方可以显示一条可折叠目标：

```text
当前目标：为古树蜂场补充野花  2 / 4          [定位]
```

- 点击“定位”让地图平滑移动到相关建筑或区域。
- 目标完成后显示一次领取入口，然后折叠为下一目标。
- 新手阶段允许目标条常驻；教程结束后玩家可以收起。

### 8.3 上下 UI 的占用原则

- 顶部 HUD 和底部导航不能同时超过视口高度的 22%。
- 地图在任何支持尺寸下都应保留至少 45% 的可视高度。
- 快捷卡展开时覆盖地图底部，不把整张地图向上挤压到无法操作。
- 输入法、系统手势区和浏览器工具栏变化时，需要重新计算安全高度。

## 9. 桌面端布局

适用基准：1280×720 及以上，最低支持 1024×640。

```text
┌──────────────────────────────────────────────────────────────┐
│ 工坊 / 等级 │ 绿宝石 能源 仓库 蜂蜜 木材 │ 环境 保存 设置    │ 56～64px
├──────────────────────────────────────────────────────────────┤
│ [筛选]                                        [事件/通知]    │
│                                                              │
│                       林业谷地地图                           │
│                                                              │
│                                        [选中建筑快捷卡]       │
├──────────────────────────────────────────────────────────────┤
│ 当前目标         地图 调查 队列 委托 仓库 更多               │ 64～72px
└──────────────────────────────────────────────────────────────┘
```

### 9.1 尺寸建议

| 区域 | 建议尺寸 |
| --- | --- |
| 顶部 HUD | 56～64px |
| 底部导航 | 64～72px |
| 地图 | 填满剩余空间 |
| 建筑快捷卡 | 320～420px 宽，最多 220px 高 |
| 任务/事件抽屉 | 320～380px 宽 |
| 设施工作台 | 视口的 72%～92% |

### 9.2 桌面端面板规则

- 不保留常驻左侧导航栏，避免压缩地图。
- 地图筛选使用左上角小型展开面板。
- 事件和通知使用右上角图标，展开后形成右侧抽屉。
- 建筑快捷卡优先停靠在右下方；与建筑重叠时自动换到左下方。
- 设施工作台打开后，地图保留在后方并降低亮度，关闭后恢复原镜头。

## 10. Android 竖屏布局

目标基准：360×800、393×873、412×915；最低支持 320px 宽。

```text
┌──────────────────────────┐
│ 工坊 / 等级      能源 60 │  48～52px
├──────────────────────────┤
│ 绿宝石  仓库  蜂蜜  木材  │  40～44px，可横向滑动
├──────────────────────────┤
│                          │
│      当前分区地图         │
│   拖动 / 缩放 / 选建筑    │
│                          │
├──────────────────────────┤
│ 建筑快捷卡 / 当前目标      │  折叠 56px，展开不超过 34vh
├──────────────────────────┤
│ 地图 调查 队列 委托 仓库 更多│ 56～64px + 安全区
└──────────────────────────┘
```

### 10.1 竖屏规则

- 顶部身份条和资源条分为两行，避免文字被压缩。
- 资源条可横向滑动，但能源、绿宝石和仓库必须始终可见。
- 地图默认聚焦当前建筑分区，不强行在窄屏一次显示整个河谷。
- 建筑透明点击区至少 48×48dp，点击区可以大于建筑图像。
- 快捷卡从底部上拉，折叠时只显示建筑名、状态和主按钮。
- 设施工作台使用全屏页面或高度不超过 92%的底部抽屉。
- 底部导航加入 `safe-area-inset-bottom`，所有主按钮保持至少 48px 高。

### 10.2 Android 返回键

返回顺序必须固定：

1. 关闭确认弹窗或结算层。
2. 关闭设施工作台或详情抽屉。
3. 收起建筑快捷卡并取消选择。
4. 地图已处于根状态时，弹出退出确认。

不能从设施页面一次返回就直接退出游戏。

## 11. 设施页面与地图的关系

设施页面不是新的根页面，而是地图上的工作层。

```text
地图根页面
├─ 古树蜂场 → 蜜蜂生产 / 培育 / 杂交
├─ 林木培育场 → 树苗生产 / 杂交 / 果树
├─ 加工工坊 → 离心 / 榨汁 / 发酵 / 蒸馏
├─ 村民商店 → 购买 / 出售 / 每日交易
├─ 生态档案馆 → 蜂 / 树 / 蝶 / 谱系
├─ 仓库能源站 → 库存 / 容量 / 能源升级
└─ 中央工作站 → 委托 / 研究 / 工坊总览
```

打开设施时必须保留：

- 地图镜头位置。
- 当前选中的建筑。
- 地图筛选状态。
- 未提交的安全选择，例如正在比较的亲本。

关闭设施后返回同一建筑，而不是回到地图左上角或刷新整个页面。

## 12. 弹窗与抽屉层级

| 内容 | 桌面端 | 手机竖屏 |
| --- | --- | --- |
| 建筑快捷信息 | 地图内浮动卡 | 底部折叠卡 |
| 设施工作台 | 中央大面板或右侧工作层 | 全屏页面 |
| NPC 对话 | 建筑附近对话卡 | 底部对话抽屉 |
| 商店交易确认 | 中央小弹窗 | 底部确认抽屉 |
| 调查模式选择 | 中央弹窗 | 底部抽屉 |
| 手动调查小游戏 | 全屏对话层 | 全屏对话层 |
| 生产结算 | 中央结算卡 | 底部结算卡 |
| 设置/存档/关于 | 右侧系统抽屉 | 全屏系统页 |

同一时间只允许一个 L8 阻塞层。不能出现“设施工作台上再叠商店，再叠确认，再叠教程”的多重弹窗链。

## 13. 新手引导在地图中的表现

教程不跳离地图，通过聚焦建筑和上下 UI 完成：

1. 聚焦中央工作站，介绍地图拖动与建筑选择。
2. 引导点击野外出口，完成第一次调查。
3. 返回地图，目标条定位古树蜂场。
4. 进入蜂场完成一次保证成功的培育。
5. 返回地图观察建筑状态变化和可收取角标。
6. 引导进入加工工坊，闭合第一条资源链。

引导层必须保留以下退出方式：

- 暂时收起教程。
- 查看当前步骤。
- 重新定位目标建筑。
- 跳过非必要说明。

教程不能阻止玩家打开设置、存档和无障碍选项。

## 14. UI 可读性与视觉规则

- 延续深林绿背景、灰绿工作板、蜂蜜金主操作、青色能源与生态状态。
- 所有信息板使用直角像素边框，不使用大圆角和玻璃模糊。
- 地图上的长文字必须有实色底板，正文对比度目标至少 4.5:1。
- 正文和按钮文字不小于 14px；手机关键按钮推荐 16px。
- 像素图标使用整数倍缩放和 `image-rendering: pixelated`。
- 图标不能作为唯一说明，资源、状态和操作必须有文字名称。
- 状态不能只依赖颜色；至少同时使用图标、文字、边框或结构中的两种。
- 顶部、底部与弹窗不得遮挡系统状态栏、浏览器手势区或 Android 安全区。

## 15. 信息优先级

### 15.1 默认可见

- 当前能源、绿宝石和仓库状态。
- 一个当前目标。
- 建筑的可收取、阻塞和升级状态。
- 当前选中建筑的一个主操作。

### 15.2 点击后可见

- 建筑详细产物与消耗。
- 环境对生产的影响。
- 队列、委托和区域收益比较。
- NPC 对话与任务交付。

### 15.3 深层页面可见

- 完整基因信息。
- 全部物品、配方和价格。
- 历史生产记录。
- 成就、图鉴和长期统计。

地图首页不能同时展开所有三级和四级信息。

## 16. 响应式断点

| 宽度 | 布局策略 |
| --- | --- |
| `≥ 1440px` | 完整顶部资源条，建筑快捷卡浮在地图右下方 |
| `1024～1439px` | 合并低频资源，事件使用抽屉 |
| `768～1023px` | 顶部资源分两组，详情改为底部横向面板 |
| `480～767px` | 手机布局，地图分区镜头，底部抽屉 |
| `< 480px` | 六按钮紧凑底栏，资源横向滚动，设施全屏 |

高度不足时优先压缩顶部辅助资源和快捷卡，不压缩底部触控区，也不让地图高度低于可操作下限。

## 17. 关键状态流程

### 17.1 点击建筑

```text
点击建筑
  ↓
地图聚焦 + 显示快捷卡
  ├─ 收取 → 地图反馈 → 更新资源栏
  ├─ 快速启动 → 显示消耗 → 开始生产
  └─ 进入设施 → 打开 L7 工作台
```

### 17.2 开始调查

```text
点击区域出口或底栏“调查”
  ↓
世界区域图
  ↓
选择区域
  ↓
手动 / 自动、收益 / 消耗比较
  ↓
二次确认
  ↓
调查或小游戏
  ↓
结算后回到地图
```

### 17.3 系统菜单

```text
底栏“更多”或顶部设置
  ↓
设置 / 存档 / 关于 / 外部链接
  ↓
关闭后返回原地图镜头
```

## 18. 数据状态建议

地图 UI 至少保存以下界面状态：

```js
{
  rootView: "map",
  camera: {
    anchor: "all",
    x: 0,
    y: 0,
    zoom: 1
  },
  selectedTarget: {
    kind: "building",
    id: "apiary"
  },
  activeMapFilter: "all",
  openLayer: null,
  collapsedObjective: false,
  resourceStripOffset: 0,
  tutorialFocus: null
}
```

玩法数据与地图 UI 状态分离。地图只读取建筑、生产、环境和任务状态，不在表现层独立计算产物。

## 19. 首版实施范围

### P0 必须完成

1. 启动后直接进入地图。
2. 顶部资源栏和底部六入口导航。
3. 建筑选择、快捷卡和进入设施。
4. 桌面端与 Android 竖屏响应式布局。
5. 设置、存档、关于移入系统菜单。
6. 新玩家在地图上的初始化与第一段教程。
7. 返回键、Esc、遮罩和安全区规则。

### P1 可以补充

1. 地图筛选与物流可视化。
2. NPC 地图对话和任务定位。
3. 资源变化动画与离线结算摘要。
4. 多镜头预设和地图事件。
5. UI 布局自定义和低性能模式。

## 20. 验收标准

### 启动与导航

- 启动后无需经过标题页即可看到地图。
- 有存档时能恢复原镜头和选中状态。
- 新玩家能在地图上完成初始化，不会面对空白或无目标状态。
- 从任意设施返回后仍停留在原建筑位置。

### 布局

- 桌面端地图是最大视觉区域，不存在常驻左侧导航挤压地图。
- 手机竖屏顶部和底部 UI 不遮挡地图核心建筑。
- 320px 宽度下不出现横向页面溢出。
- 所有底部操作避开 Android 安全区。

### 操作

- 建筑一次点击可以理解当前状态，主操作不超过一次额外点击。
- 地图拖动与建筑点击不会互相误触。
- Android 返回键按弹窗、工作台、选择、退出的顺序工作。
- 不依赖悬停、右键、双击或长按完成必要玩法。

### 可读性

- 顶部和底部文字不会直接压在复杂地图底图上。
- 关键正文和按钮在桌面端、手机端都不小于 14px。
- 普通文字对比度目标至少 4.5:1。
- 可执行、运行中、阻塞、锁定和完成状态均同时使用文字与视觉标记。

## 21. 最终设计原则

1. 地图是游戏本体，不是附加展示页。
2. 顶部回答“我拥有什么、环境如何”，底部回答“我要去哪里、接下来做什么”。
3. 建筑承担玩法入口，底栏承担全局入口，两者不重复堆满按钮。
4. 地图只显示短状态，复杂规则进入快捷卡、工作台和档案。
5. 桌面端保持大地图，手机端保持大点击区和清晰的底部抽屉。
6. 所有设施关闭后都返回原地图位置，维持空间感和经营连续性。
