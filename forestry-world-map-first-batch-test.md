# 林业谷地分层地图：首批资源测试报告

> 测试日期：2026-08-23  
> 测试性质：美术生产流程与 Web 合成验证  
> 接入状态：仅隔离测试，未接入正式游戏代码、存档或数值系统

## 1. 测试目标

首批不批量生产完整地图资源，只用四类最小样本验证以下问题：

1. 静态底图能否承载建筑、角色和 UI 三类独立图层。
2. 建筑不同状态能否在同一位置切换，不产生明显跳动。
3. 村民序列帧能否保持规则网格、透明背景和四方向循环。
4. 地点详情弹窗的层级、对比度和按钮基线是否适合后续 HTML/CSS 重建。
5. AI 生成资源是否适合直接进入批量生产，或需要先建立更严格的校正工序。

隔离测试页：`map-layer-test.html`

## 2. 测试结论

| 项目 | 技术结果 | 美术结果 | 结论 |
| --- | --- | --- | --- |
| 夏季静态底图 | 1672×941，24 位 RGB | 道路、空地和生态分区清晰，无建筑、人物、文字 | 通过 |
| 蜂场三状态条 | 2172×724，三等分后每格 724×724，四角 Alpha=0 | 状态辨识清楚，但各状态视觉体积和锚点不够稳定，边缘有红色杂点 | 不通过，需重制 |
| 普通村民四方向序列帧 | 1536×1024，6×4 网格，每格 256×256，四角 Alpha=0 | 方向和身份一致，动作可循环；人物偏精细，需继续检查地图缩放后的识别度与抖动 | 条件通过 |
| 地点详情弹窗 | 1536×1024，32 位 ARGB，四角 Alpha=0 | 信息层级、图标避让、按钮基线和对比度清楚 | 通过，后续用 HTML/CSS 重建 |

首轮总体判断：分层方向成立，但建筑资源不能直接批量生成。必须先把“固定画布、固定锚点、固定占地、边缘清理”变成独立校正工序，再扩展到全建筑和四季版本。

## 3. 资源清单

### 3.1 夏季静态底图

- 文件：`assets/map/test-batch/base/valley-base-summer-test-v1.png`
- 尺寸：1672×941
- 像素格式：Format24bppRgb
- SHA-256：`3E99D728FE90F0EC588B79FD61FDD537A7D7CAB580DF342A6F06895737B28E4C`
- 结果：通过

通过原因：

- 具备森林、树场、中央任务广场、河流、湿地、矿区、雪线和沙地区域。
- 道路能连接主要功能区。
- 预留了多个可替换建筑空地。
- 没有建筑、人物、UI 或文字污染静态层。

后续注意：正式版需要重新确定所有建筑空地的像素坐标、占地尺寸和遮挡关系，并为春、秋、冬复用同一构图。

### 3.2 蜂场三状态条

- 文件：`assets/map/test-batch/buildings/apiary/apiary-state-strip-summer-test-v1.png`
- 尺寸：2172×724
- 网格：3×1
- 单格：724×724
- 像素格式：Format32bppArgb
- 四角 Alpha：0、0、0、0
- SHA-256：`13F5B4CE8C853905BD19807B99D1BB79832A7F680567C2919E5EB950C6BCB998`
- 结果：不通过

问题：

1. 建设空地、LV1、LV2 的视觉包围盒逐级放大，切换时会产生跳动。
2. 三个状态的底部落点和入口位置没有做到像素级一致。
3. 建筑边缘出现少量红色杂点，叠加到地图后较明显。
4. 升级体积变化过大，可能侵入相邻道路或交互热点。

修正方案：

1. 不再一次生成三状态条，先生成 LV1 主参考。
2. 用 LV1 作为参考图，分别编辑出“建设空地”和“LV2”，每张都使用相同 724×724 透明画布。
3. 在生成后增加包围盒检测、底部锚点对齐和边缘杂色检查。
4. 最终再机械合并为状态条，避免生成器自行改变网格和比例。

### 3.3 普通村民四方向序列帧

- 文件：`assets/map/test-batch/characters/villager/villager-walk-4dir-test-v1.png`
- 尺寸：1536×1024
- 网格：6×4
- 单格：256×256
- 像素格式：Format32bppArgb
- 四角 Alpha：0、0、0、0
- SHA-256：`A60F435DED779894AE5AD746F90C1C3E505FAE58DBB9053C29C534DCA47D6590`
- 结果：条件通过

说明：

- 第一行向南、第二行向东、第三行向北、第四行向西。
- 每行 6 帧，可由 CSS `steps(6)` 驱动。
- 透明像素中保留了棕色 RGB 数据，但 Alpha 为 0，浏览器合成时不会显示背景。
- 正式导出前仍建议清理透明像素 RGB，避免某些图片工具在缩放或预乘 Alpha 时产生棕色边缘。

需要继续观察：

1. 六帧脚底锚点是否造成上下抖动。
2. 地图实际显示到约 32–48 像素高时，脸部和挎包是否过于细碎。
3. 角色风格是否需要再向方块感、低像素密度靠拢。

### 3.4 地点详情弹窗

- 文件：`assets/map/test-batch/ui/location-popup-test-v1.png`
- 尺寸：1536×1024
- 像素格式：Format32bppArgb
- 四角 Alpha：0、0、0、0
- SHA-256：`3D4B58EB4EE7DD8A757C90D736C34FAFE78FF622820F4D0F69D9E7847B1256EA`
- 结果：通过

通过原因：

- 标题区、建筑缩略图区、资源信息区和操作区层级明确。
- 三条资源信息没有被图标遮挡。
- 两个底部按钮高度和基线一致。
- 深绿、灰绿、奶油白和蜂蜜金之间对比度清楚。

使用原则：该图片只作为 UI 视觉参考，不直接作为整张弹窗贴图。正式版本应使用 HTML/CSS 重建框架，只复用小图标和缩略图，以适配 Web 与安卓竖屏。

## 4. 隔离预览页验证内容

`map-layer-test.html` 提供以下交互：

1. 显示夏季静态底图。
2. 在同一地图位置切换建设空地、蜂场 LV1、蜂场 LV2。
3. 在同一角色节点切换向南、向东、向北、向西四行序列帧。
4. 使用 CSS `steps(6)` 播放村民步行动画。
5. 单独查看地点详情弹窗和透明边缘。
6. 在桌面宽屏与窄屏下检查布局。

自动检查结果：

- 测试页 HTTP 状态：200，`text/html; charset=utf-8`。
- 四张测试 PNG HTTP 状态：全部 200，`image/png`。
- 页面资源路径：全部存在。
- 交互脚本语法：`JS_PARSE_OK`。
- 当前 Windows 浏览器自动化连接失败，因此最终视觉点击检查需在浏览器中人工完成一次；不影响以上文件、HTTP、尺寸、网格和 Alpha 检查结果。

本页不包含：

- 建筑点击与升级消耗；
- NPC 寻路与碰撞；
- 地点弹窗的实际 HTML/CSS 交互；
- 存档、资源变化、任务或季节切换；
- 正式游戏入口。

## 5. 本批生成提示词记录

生成方式：Codex 内置 ImageGen；四项均为原创像素美术测试资源。

### 5.1 夏季静态底图提示词要点

- 16:9 夏季静态地形，不含建筑、人物、UI、文字。
- 中央任务广场与放射状土路。
- 七处主要建设空地。
- 左上密林、左中树场、右侧河流与湿地、左侧矿洞、北部雪线、右下沙地、右上空置传送平台。
- 三分之四俯视、硬像素边缘、固定光源、原创方块世界材质语言。

### 5.2 蜂场状态条提示词要点

- 真透明 PNG，精确 3×1 等分网格。
- 同一三分之四俯视角度、占地中心、入口方向和底部锚点。
- 第一格建设空地，第二格 LV1 小型木制蜂场，第三格 LV2 扩建蜂场。
- 夏季花草、蜂箱和蜂巢符号；无地图、人物、UI、文字或分隔线。
- 强调三状态必须是同一建筑的升级过程。

### 5.3 村民序列帧提示词要点

- 真透明 PNG，精确 6×4 网格，共 24 帧。
- 行顺序：南、东、北、西；每行六帧完整步行循环。
- 每帧固定脚底中心锚点、比例、身份、服装、光源和透明留白。
- 苔绿工作服、棕裤、短靴、腰带和同一肩侧挎包。
- 禁止网格线、标签、地面、阴影、场景或越过相邻格的物件。

### 5.4 地点详情弹窗提示词要点

- 真透明背景，只生成一个独立横向弹窗。
- 深绿方角外框、灰绿内面板、奶油色占位条、蜂蜜金主按钮。
- 标题区、建筑缩略图、进度条、三条资源信息和底部两个等高按钮。
- 不烘焙可读中英文，只用图标和中性占位条。
- 禁止圆角、玻璃效果、模糊、渐变、场景或第二个弹窗。

## 6. 首批验收门槛

在进入第二批之前，建议满足以下条件：

- [x] 夏季底图构图可用。
- [x] 村民序列帧网格和 Alpha 可用。
- [x] UI 视觉层级可用。
- [ ] 建筑三状态包围盒偏差小于单格宽高的 4%。
- [ ] 建筑底部锚点偏差不超过 2 个最终显示像素。
- [ ] 建筑透明边缘无红色或其他无关杂色。
- [ ] 桌面和安卓竖屏预览时，建筑不遮挡主要道路与地点按钮。

## 7. 下一批建议

先完成“蜂场状态条 v2”，不要立刻扩展全部建筑。v2 通过后再制作：

1. 树场：未建设、LV1、LV2，夏季。
2. 加工房：未建设、LV1、LV2，夏季。
3. 商店：未建设、LV1、LV2，夏季。
4. 蜂场春、秋、冬三套季节覆盖层。
5. Hivekeeper NPC 的待机与四方向步行小样。

只有当蜂场、树场两类建筑都能稳定切换且不跳动，再进入完整建筑矩阵和四季批量生产。

## 8. v2 相机与比例校准

根据用户提供的新参考图，首批测试增加以下强制标尺：

- 相机：正交三分之四俯视，约从地面上方 55° 观察，不出现消失点。
- 朝向：从南偏西看向北偏东，建筑正面朝画面下方。
- 地面：道路宽度不随距离收缩，建筑空地在纵向按同一角度压缩。
- 建筑：在 1672×941 地图中，普通工作坊合成高度约 140–190px。
- 村民：地图合成高度约 28–36px，约 3.5 头身；高度约为普通工作坊的 1/4–1/6。

### v2 结果

| 资源 | 结果 | 说明 |
| --- | --- | --- |
| `valley-base-summer-test-v2.png` | 视角通过 | 树木、悬崖、空地、道路与河岸投影统一，视觉密度接近参考图 |
| `apiary-state-strip-summer-test-v2.png` | 视角通过，Alpha 不通过 | 屋顶压缩、正面高度和升级幅度已收敛，但输出为绘制的棋盘格实底 |
| `villager-walk-4dir-test-v2.png` | 比例通过，Alpha 不通过 | 头部缩小、身体变窄、腿部加长，按 30–34px 合成；输出仍为棋盘格实底 |

文件校验：

- 底图 v2：1672×941，SHA-256 `308FD9F02A8401FF17E72252D97EFCC4D0D696A70CD2704D1C42414DA06AFFAB`
- 蜂场 v2：1774×887，Format24bppRgb，SHA-256 `BF61F733896262CEC3E4B6B91A9E697908990B44EDF4269FAC1A07384912D726`
- 村民 v2：1536×1024，6×4 网格，每格 256×256，Format24bppRgb，SHA-256 `DB8C309E5D230446D99F5BCD69714376292DF66E1864BBF77430CAA469538C10`

### 当前预览策略

`map-layer-test.html` 已切换到 v2 底图、蜂场和村民。蜂场与村民的白色实底在预览中暂时使用 CSS `mix-blend-mode: multiply` 消隐，仅用于判断视角与比例，不能视为透明资源交付。

### 下一道门槛

1. 先由页面预览确认相机与人物比例。
2. 确认后将蜂场三个状态拆成三张独立、固定画布的透明图，不再直接生成状态条。
3. 村民同样先制作单方向六帧透明小样，通过后再扩展四方向。
4. Alpha、锚点和包围盒全部通过后，才开始其他建筑和 NPC 的批量生产。

## 9. v3 降噪与叠加修复

问题参考：`assets/map/test-batch/references/overlay-problem-reference-v2.png`

用户反馈的问题：

1. `mix-blend-mode: multiply` 会把蜂场整体压黑，白色或棋盘格底也会污染地图。
2. 蜂场错误地落在下方道路与生产区之间，没有占用林区空地。
3. 蜂场屋顶过陡、正面墙体过高，与底图的高俯视角不一致。
4. v2 底图含有过量草点、花点、石点和高光，缩放后形成明显视觉噪声。

### 修正内容

- 新底图：`assets/map/test-batch/base/valley-base-summer-test-v3.png`
  - 1672×941，SHA-256 `AB6BCD55B4935EE6979503B3DE769F6065E15DB748BEAB335CAAA0BE2F7EB801`
  - 减少约 65% 微小地表纹理，使用更大的树冠、道路、草地和水面色块。
  - 清理建设空地，让建筑轮廓更容易读取。
- 新蜂场：`assets/map/test-batch/buildings/apiary/apiary-state-strip-summer-test-v3-chroma.png`
  - 1774×887，SHA-256 `F0A7DF8DF127EE3A0897E553C555744E084F810EB91AE12BDC2DADB0B7935F9D`
  - 提高俯视角，压低正面墙体，减少木材和屋顶微纹理。
  - 使用纯洋红键控底，避免生成器重复输出伪透明棋盘格。
- 蜂场位置：左侧约 27%、顶部约 2%，对应左上林区第一块空地；桌面宽度约为地图的 14%。
- 村民显示：单帧容器降到 34px，人物实际可见约 24–28px，并移动到林区道路旁。

### 新叠加流程

`map-layer-test.js` 在浏览器加载后执行：

1. 把蜂场状态条等分为三个画布。
2. 清除洋红键控像素，生成三张带真实 Alpha 的运行时 PNG 数据。
3. 切换建筑按钮时只替换已透明化的状态图，不再使用混合模式。
4. 从村民序列帧画布边缘泛洪清除相连的近白背景，保留角色内部亮色。
5. 透明化完成前隐藏图层，避免短暂出现洋红或白色方块。

### 验证

- `map-layer-test.js`：Node 语法检查通过。
- 页面、脚本、v3 底图、蜂场键控图和村民序列帧：本地 HTTP 状态全部 200。
- 页面已无 `mix-blend-mode: multiply`。
