# 林业谷地 2.5D 网格地图逻辑

> 版本：P0 原型 01  
> 日期：2026-08-23  
> 对应页面：`map-grid-layout-test.html`

## 1. 重构目标

地图不再使用“图片上的百分比坐标 + CSS 目测位置”。背景图只负责视觉，所有建筑、NPC、碰撞、寻路和前后遮挡都绑定到独立的 2.5D 逻辑网格。

本轮解决三个问题：

1. 建筑位置可由玩家/设计者在网格上直接选择，不再写死。
2. 建筑使用底部中心锚点，并可独立调整缩放、纵向压缩和垂直偏移。
3. 村民使用实际网格路径移动，序列帧通过整数帧索引切换，不再连续滚动整张贴图。

## 2. 坐标系统

### 2.1 地图坐标

- 设计分辨率：1667×943。
- 核心逻辑网格：25×25；全场景选择范围延伸为 `-14..39`。
- 既有坐标：`gx = 0..24`、`gy = 0..24` 保持兼容，扩展坐标使用同一投影公式。
- 网格原点：`originX = 833.5`，`originY = 28`。
- 单格菱形半宽：34px。
- 单格菱形半高：18px。

网格形成覆盖完整画布的正交菱形地面；页面裁除画布外网格，但保留矿区、传送台和沙地等边角区域的选择能力。

### 2.2 投影公式

```text
screenX = originX + (gx - gy) × tileHalfWidth
screenY = originY + (gx + gy) × tileHalfHeight
```

当前参数代入后：

```text
screenX = 833.5 + (gx - gy) × 34
screenY = 28  + (gx + gy) × 18
```

这套坐标不会改变背景图，只在背景上建立稳定的逻辑地面。

## 3. 图层结构

从后到前：

1. `map-background`：夏季静态背景。
2. `iso-grid`：可开关的定位网格。
3. 建筑与村民：根据脚底/地基锚点动态排序。
4. 已保存候选位标记。
5. 选择光标和交互提示。

建筑和 NPC 的层级根据投影后的 `screenY` 计算：

```text
zIndex = baseLayer + round(screenY)
```

因此越靠近画面下方的对象越靠前，可自动形成 2.5D 前后遮挡。

## 4. 建筑逻辑

### 4.1 锚点

所有建筑统一使用“地基底部中心”锚点：

- 网格只记录建筑地基的位置。
- 建筑图片向左平移自身宽度的 50%。
- 建筑图片向上平移到地基底线。
- 建筑升级时不得改变锚点。

### 4.2 视觉校准参数

当前实验室提供三项参数：

| 参数 | 默认值 | 用途 |
| --- | ---: | --- |
| 缩放 | 100% | 控制建筑在地图中的总体尺寸 |
| 纵向压缩 | 100% | v3 建筑已按原生 2.5D 生成，滑杆仅用于微调 |
| 锚点微调 | 0px | 修正图片透明留白或地基底线误差 |

这些参数与网格坐标一起保存，后续可直接转换为游戏数据。

### 4.3 建筑位置数据

```json
{
  "building": "apiary",
  "gx": 4,
  "gy": 8,
  "state": 1,
  "scale": 1,
  "pitch": 0.8,
  "offsetY": 0
}
```

在定位实验室点击“保存当前建筑位”后，数据写入浏览器本地存储，并可导出为 JSON。

## 5. 村民移动逻辑

### 5.1 路径

村民以网格单元为移动单位：

1. 玩家在“村民移动模式”点击目标格。
2. 系统从村民当前格生成逐格路径。
3. X、Y 两个网格轴交替前进，减少长直角折线的生硬感。
4. 每格移动约 260ms。
5. 屏幕位置在相邻两个投影坐标之间平滑插值。

当前为基础路径测试，尚未加入障碍、道路权重和 A* 寻路。

### 5.2 序列帧

序列帧表为 6×4：

| 行 | 方向 |
| ---: | --- |
| 0 | 向南 |
| 1 | 向东 |
| 2 | 向北 |
| 3 | 向西 |

动画不再使用 CSS 连续改变背景位置。JavaScript 每次只设置整数帧：

```text
frameX = frame × 20%
frameY = directionRow × 33.333%
```

这样每次只显示一格，不会出现整张序列图横向滚动的状态。停止移动时回到当前方向的第 0 帧。

### 5.3 比例

- 村民在设计分辨率下的目标显示高度：约 28–36px。
- 普通工作建筑目标高度：约 140–190px。
- 村民约为建筑可见高度的 1/4–1/6。

页面缩放时，建筑和村民均按 `stageWidth / 1667` 同比缩放。

## 6. 建筑位置确认流程

1. 打开 `map-grid-layout-test.html`。
2. 保持“建筑定位模式”。
3. 点击网格，把蜂场移动到一个背景空地。
4. 调整缩放、纵向压缩和锚点微调。
5. 点击“保存当前建筑位”。
6. 重复选择多个候选位置。
7. 点击右侧候选位或地图编号，可以返回该位置继续调整。
8. 点击“导出 JSON”复制最终布局。

候选位置保存在：

```text
localStorage["forestry-map-layout-2_5d-v1"]
```

## 7. 当前限制

1. 背景图本身不是严格按菱形瓦片绘制，因此网格只作为逻辑层，不要求道路完全沿格线。
2. 蜂场四个状态、首版其余六座 LV1 和村民 v3 已使用真实 ARGB；高分辨率测试图仍需在正式资源阶段统一到目标像素尺寸。
3. 其余六座建筑尚未生成独立 LV2、LV3 与季节版本，当前只用于选址和比例确认。
4. 村民尚未加入碰撞、道路优先级、等待、转向停顿和随机散步。
5. 扩展网格已覆盖地图边角，但尚未标记水域、悬崖、道路权重和不可建造区域。

## 8. 下一步

建筑位置确认后再进行以下工作：

1. 固化建筑槽位表和不可行走区域。
2. 按最终缩放和纵向压缩值重绘透明蜂场素材。
3. 将道路格标记为低移动成本，草地为普通成本，水域、悬崖和建筑地基为阻挡。
4. 使用 A* 替换当前基础逐格路径。
5. 加入 NPC 随机散步、停留、对话触发和建筑入口点。
6. 将确认后的布局数据接入正式地图页面。

## 9. 已确认建筑槽位 v1

用户导出的布局已保存为：

```text
assets/map/layouts/forestry-map-layout-v1.json
```

`building: "apiary"` 表示定位时使用蜂场作为预览模型，不代表最终需要同时放置五座蜂场。五组坐标作为通用建筑槽位，后续再分配蜂场、树场、加工区、商店或研究所。

| 槽位 | 网格坐标 | 屏幕锚点 | 当前缩放 | 当前纵向压缩 |
| --- | --- | --- | ---: | ---: |
| S01 | `(3, 6)` | `(731.5, 190)` | 100% | 100% |
| S02 | `(8, 7)` | `(867.5, 298)` | 100% | 100% |
| S03 | `(13, 21)` | `(561.5, 640)` | 100% | 100% |
| S04 | `(18, 21)` | `(731.5, 730)` | 100% | 100% |
| S05 | `(22, 15)` | `(1071.5, 694)` | 100% | 100% |

网格实验室行为：

1. 浏览器没有本地布局时，自动加载以上五个槽位。
2. 用户继续调整后，以浏览器本地存储为优先。
3. 点击“恢复布局 v1”可重新载入这五个槽位。
4. 点击“导出 JSON”仍输出当前本地版本，不会覆盖项目内的布局文件。

## 10. 全场景投影与首版建筑素材更新

更新日期：2026-08-24。

### 10.1 全场景扩展网格

- `0..24` 继续作为核心 25×25 网格，原有五个槽位坐标不变。
- 可选坐标扩为 `-14..39`，使用同一套 `screenX / screenY` 投影公式。
- 页面只绘制与 1667×943 画布相交的菱形格，避免生成不可见节点。
- 扩展范围覆盖右上传送平台、左下机器区和右下沙地空场。

导出 JSON 在原有 `gridSize: 25` 外新增 `gridRange: [-14, 39]`，旧布局仍可读取。

### 10.2 首版七座建筑

| ID | 中文名 | 本轮可选素材 |
| --- | --- | --- |
| `apiary` | 古树蜂场 | 工地、LV1、LV2、LV3 |
| `station` | 中央工作站 | 通用工地、LV1 |
| `arbor` | 林木培育场 | 通用工地、LV1 |
| `processing` | 加工工坊 | 通用工地、LV1 |
| `warehouse` | 仓库能源站 | 通用工地、LV1 |
| `market` | 村民商店 | 通用工地、LV1 |
| `archive` | 生态档案馆 | 通用工地、LV1 |

所有新素材均为独立 32 位 ARGB PNG，并按自身画布宽高比、透明下边距和底部中心锚点显示。

### 10.3 蜂场四阶段

蜂场状态轴固定为四档：

- `site`：建设工地；
- `lv1`：基础养蜂棚；
- `lv2`：扩建蜂房；
- `lv3`：带观察室的完整蜂业研究屋。

其余六座建筑本轮先提供 LV1 用于选址；确认位置和比例后再按同一锚点补齐各自 LV2、LV3。

## 11. 用户确认建筑布局 v2

更新日期：2026-08-24。用户导出的七建筑布局已保存为：

```text
assets/map/layouts/forestry-map-layout-v2.json
```

| 建筑 | 网格坐标 | 屏幕锚点 | 缩放 |
| --- | --- | --- | ---: |
| 加工工坊 | `(15, 30)` | `(323.5, 838)` | 130% |
| 仓库能源站 | `(33, 14)` | `(1479.5, 874)` | 79% |
| 中央工作站 | `(8, 7)` | `(867.5, 298)` | 112% |
| 古树蜂场 | `(22, 15)` | `(1071.5, 694)` | 76% |
| 村民商店 | `(25, 21)` | `(969.5, 856)` | 90% |
| 生态档案馆 | `(13, 21)` | `(561.5, 640)` | 92% |
| 林木培育场 | `(4, 8)` | `(697.5, 244)` | 111% |

页面行为：

1. 默认进入“完整布局”模式，同时显示七座建筑并隐藏网格、编号和当前编辑预览。
2. 点击“进入定位编辑”后恢复网格、槽位编号和单建筑调整控件。
3. 已放置建筑按屏幕锚点 Y 值排序，保持正确的 2.5D 前后遮挡。
4. 定位编辑模式保留独立菱形地基辅助层；完整布局模式隐藏该层，直接使用 v4 底图中已统一绘制的地块，避免灰色辅助底板从建筑脚下露出。
5. 旧布局未使用的中央裸地回填为像素草甸；河流、桥梁、传送平台和中央广场保持不变。

建筑风格统一后的夏季底图保存为：

```text
assets/map/test-batch/base/valley-base-summer-test-v4.png
```

v4 以现有建筑的轮廓密度、材质分块、明暗方向和像素簇为参考，强化草地、道路、水体、岩层、林地与花圃的可读性，同时继续使用布局 v2 的七个建筑锚点。

静态合成预览保存为：

```text
assets/map/previews/forestry-valley-layout-v3-preview.png
```
